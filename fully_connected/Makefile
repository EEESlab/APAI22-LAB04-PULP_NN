# APPLICATION NAME
APP = fullyConnected

# SOURCE FILES
APP_SRCS = main.c

# NUMBER OF USED CLUSTER CORES
CORES ?= 1

# USE SIMD IMPLEMENTATION OF FULLYCONNECTED LAYER
SIMD ?= 0

# COMPILER FLAGS
APP_CFLAGS += -O3 -Iinclude -DNUM_CORES=$(CORES) -DUSE_SIMD=$(SIMD)
APP_LDFLAGS +=

include $(RULES_DIR)/pmsis_rules.mk
